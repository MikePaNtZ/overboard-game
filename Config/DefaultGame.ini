[/Script/EngineSettings.GeneralProjectSettings]
ProjectID=
ProjectName=Overboard
Description=Renders board pose received over UDP from the overboard controls host; computes no physics itself (ADR-0009).

; Meshes/openwheel/*.stl are loose files loaded at runtime by ABoardActor (see mesh/README.md),
; not imported .uasset content, so they need to be staged explicitly to ship in a packaged build.
; Added preventively alongside the W3 mesh work; NOT verified against an actual package/cook --
; no display in this environment to run one. Check this is still correct before Monday's build if
; someone packages rather than runs PIE.
[/Script/UnrealEd.ProjectPackagingSettings]
+DirectoriesToAlwaysStageAsUFS=(Path="Meshes")
